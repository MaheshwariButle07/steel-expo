/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['pzcyatdoqigagyfbsani.supabase.co'], // Ensure this domain is correct
      },

};

export default nextConfig;
